<%= simple_form_for @company do |f| %>
  <%= f.input :name %>
  <%= f.label 'Parent' %>
  <%= f.collection_select :parent_id, Company.order(:name),:id,:name, include_blank: true  %>
  <%= f.input :required_employees_count, default: 0 %>
  <%= f.input :country %>
  <%= f.button :submit %>
<% end %>